1. `filter_samples_by_citation_count.py` creates `samplesTrim.db`.
2. `export_grouped_samples_pickle.py` converts `samplesGroups.db` to `samplesGroups.pkl`.
3. `build_grouped_samples.py` creates `samplesGroups.db`.
4. `analysis.py` contains lightweight inspection code for grouped samples.
